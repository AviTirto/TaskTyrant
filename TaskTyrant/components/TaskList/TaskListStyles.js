import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    appContainer: {
      flex: 1,
      padding: 20, 
    },
    listContainer: {
      flex: 1,
    },
});

export default styles
  